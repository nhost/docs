---
title: "Nhost env:ls"
---

`nhost env:ls` lists all dev environment variables.

```bash
$ nhost env:ls
Environment variables for todos-cli.

name                value (dev)
FIRST_ENV           22
ANOTHER_ONE         myAPIkey
```
