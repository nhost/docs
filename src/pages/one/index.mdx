# one index mdx

```
import React, G
import styled from "styled-components";

const IndexContainer = styled.div``;

export default function Index(props) {
  return <IndexContainer>Index</IndexContainer>;
}

```
